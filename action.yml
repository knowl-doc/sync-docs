name: 'Hello World'
description: 'Greet someone'
inputs:
  who-to-greet:  # id of input
    description: 'Who to greet'
    required: true
    default: 'World'
outputs:
  random-number:
    description: "Random number"
    value: ${{ steps.random-number-generator.outputs.random-number }}
runs:
  using: "composite"
  steps:
    - run: echo Hello ${{ inputs.who-to-greet }}.
      shell: bash
    - run: echo "${{ github.action_path }}" >> $GITHUB_PATH
      shell: bash
    - run: echo Hello ${{ inputs.who-to-greet }}.
      shell: bash
      env:
        GITHUB_TOKEN: ${{github.token}}
        GITHUB_REF: ${{github.ref}}
        CURRENT_COMMIT_SHA: ${{github.event.pull_request.head.sha}}
        RANDOM_ENV: bbc
#     - run: |
#         echo "RANDOM_ENV=bbc" >> $GITHUB_ENV
#         echo "GITHUB_TOKEN=${{github.token }}" >> $GITHUB_ENV
#         echo "GITHUB_REF=${{github.ref}} " >> $GITHUB_ENV
#         echo "CURRENT_COMMIT_SHA=${{github.event.pull_request.head.sha}}" >> $GITHUB_ENV
#       shell: bash 
    - run: sync-linked-pages.sh
      shell: bash
